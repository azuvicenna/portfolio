name: Keep Supabase Alive

on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:

jobs:
  ping-api:
    runs-on: ubuntu-latest
    steps:
      - name: Fetch About API
        run: |
          curl -I https://api-azuvicenna-portfolio.up.railway.app/api/v1/about
